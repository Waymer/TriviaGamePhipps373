<%= form_for(@record_score) do |f| %>
  <% if @record_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@record_score.errors.count, "error") %> prohibited this record_score from being saved:</h2>

      <ul>
      <% @record_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gr_id %><br>
    <%= f.number_field :gr_id %>
  </div>
  <div class="field">
    <%= f.label :qs_id %><br>
    <%= f.number_field :qs_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
